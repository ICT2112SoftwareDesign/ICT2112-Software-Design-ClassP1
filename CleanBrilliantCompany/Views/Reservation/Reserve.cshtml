@model List<Dictionary<string, object>>

@{
    ViewData["Title"] = "Reservation Product List";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<!-- Reservation Content -->
<div class="container-fluid">

    <!-- Page Heading -->
    <div class="d-sm-flex align-items-center justify-content-between mb-4">
        <h1 class="h3 mb-0 text-gray-800">Reservation Product List</h1>
    </div>

    <table class="table table-bordered table-striped">
        <thead class="table-dark">
            <tr>
                <th>Product ID</th>
                <th>Product Name</th>
                <th>Product Category</th>
                <th>Manufacturer ID</th>
                <th>Product Weight</th>
                <th>Volume</th>
                <th>Toxicity Percentage</th>
                <th>Carbon Footprint</th>
                <th>Product State</th>
                <th>Count</th>
                <th>Action</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var products in Model)
            {
            <tr>
                <td>@products["ProductId"]</td>
                <td>@products["ProductName"]</td>
                <td>@products["ProductCategory"]</td>
                <td>@products["ManufacturerId"]</td>
                <td>@products["ProductWeight"]</td>
                <td>@products["Volume"]</td>
                <td>@products["ToxicityPercentage"]</td>
                <td>@products["CarbonFootprint"]</td>
                <td>@products["ProductState"]</td>
                <td>@products["Count"]</td>
                <td class="d-flex">
                    <button type="button" class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#reserveModal"
                            data-reserveModal-id="@products["ProductId"]">
                        Reserve
                    </button>
                </td>
            </tr>
            }
        </tbody>
    </table>

    @* reserve modal *@
    <div class="modal fade" id="reserveModal" tabindex="-1" aria-labelledby="reserveModal" aria-hidden="true">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="reserveModalLabel">Create Reservation</h5>
                </div>
                <div class="modal-body">
                    <form method="post" asp-action="reserveStock">
                        <input type="hidden" id="reserveProductId" name="productId">
                        <div class="mb-3">
                            <label for="warehouseId" class="form-label">Warehouse ID</label>
                            <input type="number" class="form-control" id="warehouseId" name="warehouseId" required>
                        </div>
                        <div class="mb-3">
                            <label for="reservedQuantity" class="form-label">Reserved Quantity</label>
                            <input type="number" class="form-control" id="reservedQuantity" name="reservedQuantity" required>
                        </div>
                        <div class="mb-3">
                            <label for="reservationPurpose" class="form-label">Reservation Purpose</label>
                            <input type="text" class="form-control" id="reservationPurpose" name="reservationPurpose" required>
                        </div>
                            <input type="hidden" class="form-control" id="staffId" name="staffId" value=1>
                        <div class="modal-footer">
                            <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
                            <button type="submit" class="btn btn-primary">Reserve Product</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>

<!-- Bootstrap core JavaScript-->
<script src="vendor/jquery/jquery.min.js"></script>
<script src="vendor/bootstrap/js/bootstrap.bundle.min.js"></script>

<!-- Core plugin JavaScript-->
<script src="vendor/jquery-easing/jquery.easing.min.js"></script>

<!-- Custom scripts for all pages-->
<script src="js/sb-admin-2.min.js"></script>

<!-- Page level plugins -->
<script src="vendor/chart.js/Chart.min.js"></script>

<!-- Page level custom scripts -->
<script src="js/demo/chart-area-demo.js"></script>
<script src="js/demo/chart-pie-demo.js"></script>

<!-- My custom product JS-->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

<script>
    document.addEventListener("DOMContentLoaded", function () {
        var updateModal = document.getElementById("reserveModal");

        updateModal.addEventListener("show.bs.modal", function (event) {
            var button = event.relatedTarget;
            var productId = button.getAttribute("data-reserveModal-id");

            var hiddenInput = document.getElementById("reserveProductId");
            hiddenInput.value = productId;
            console.log(hiddenInput.value);
        });
    });

</script>

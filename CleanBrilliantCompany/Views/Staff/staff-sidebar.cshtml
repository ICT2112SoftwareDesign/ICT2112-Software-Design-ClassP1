<!-- Sidebar -->
<ul class="navbar-nav bg-gradient-primary sidebar sidebar-dark accordion" id="accordionSidebar">

    <!-- Sidebar - Brand -->
    <a class="sidebar-brand d-flex align-items-center justify-content-center my-3" href="@Url.Action("", "Staff")">
        <div class="sidebar-brand-icon rotate-n-15">
            <i class="fas fa-laugh-wink"></i>
        </div>
        <div class="sidebar-brand-text mx-3">Clean Brilliant Company</div>
    </a>

    <!-- Divider -->
    <hr class="sidebar-divider my-0">

    <!-- Nav Item - Dashboard -->
    <li class="nav-item @(ViewContext.RouteData.Values["controller"].ToString() == "Staff" && (ViewContext.RouteData.Values["action"].ToString() == "Dashboard" || ViewContext.RouteData.Values["action"].ToString() == "") ? "active" : "")">
        <a class="nav-link" href="@Url.Action("Dashboard", "Staff")">
            <i class="fas fa-fw fa-tachometer-alt"></i>
            <span>Dashboard</span>
        </a>
    </li>

    <!-- Divider -->
    <hr class="sidebar-divider">

    <!-- Heading -->
    <div class="sidebar-heading">
        Features
    </div>

    <!-- Nav Item - Shipping -->
    <li class="nav-item @(ViewContext.RouteData.Values["controller"].ToString() == "ShippingAgent" ? "active" : "")">
        <a class="nav-link @(ViewContext.RouteData.Values["controller"].ToString() == "ShippingAgent" ? "" : "collapsed")" href="@Url.Action("Index", "ShippingAgent")">
            <i class="fas fa-fw fa-ship"></i>
            <span>Shipping</span>
        </a>
    </li>

    <li class="nav-item @(ViewContext.RouteData.Values["controller"].ToString() == "OrderFulfilment" ? "active" : "")">
        <a class="nav-link @(ViewContext.RouteData.Values["controller"].ToString() == "OrderFulfilment" ? "" : "collapsed")" href="@Url.Action("Index", "OrderFulfilment")">
            <i class="fas fa-fw fa-list-alt"></i>
            <span>Order Fulfilment</span>
        </a>
    </li>

    <!-- Nav Item - Return/Refund -->
    <li class="nav-item @(ViewContext.RouteData.Values["action"].ToString() == "Refund" ? "active" : "")">
        <a class="nav-link @(ViewContext.RouteData.Values["action"].ToString() == "Refund" ? "" : "collapsed")" href="@Url.Action("Refund", "Staff")">
            <i class="fas fa-fw fa-money-bill"></i>
            <span>Return & Refund</span>
        </a>
    </li>

    <!-- Nav Item - Reorder -->
    <li class="nav-item @(ViewContext.RouteData.Values["action"].ToString() == "Reorder" ? "active" : "")">
        <a class="nav-link @(ViewContext.RouteData.Values["action"].ToString() == "Reorder" ? "" : "collapsed")" href="@Url.Action("Reorder", "Staff")">
            <i class="fas fa-fw fa-list"></i>
            <span>Reorder</span>
        </a>
    </li>

    <!-- Nav Item - Support Tickets -->
    <li class="nav-item @(ViewContext.RouteData.Values["action"].ToString() == "SupportTicket" ? "active" : "")">
        <a class="nav-link @(ViewContext.RouteData.Values["action"].ToString() == "SupportTicket" ? "" : "collapsed")" href="@Url.Action("SupportTicket", "Staff")">
            <i class="fas fa-fw fa-ticket-alt"></i>
            <span>Tickets</span>
        </a>
    </li>

    <!-- Divider -->
    <hr class="sidebar-divider d-none d-md-block">

    <!-- Sidebar Message -->
    <div class="sidebar-card d-none d-lg-flex">
        <a class="btn btn-success btn-sm" asp-controller="StaffLogin" asp-action="StaffLogout">Logout!</a>
    </div>

</ul>
<!-- End of Sidebar -->
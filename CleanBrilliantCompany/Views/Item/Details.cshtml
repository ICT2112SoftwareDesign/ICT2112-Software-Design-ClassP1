@* @model CleanBrilliantCompany.Models.Entity.Item

@{
    ViewData["Title"] = "Item Details";
}

<h1>Item Details</h1>

<ul>
    @foreach (var item in Model)  <!-- Loop through the items -->
    {
        <li>@item.Name - @item.Description</li> <!-- Display item properties -->
    }
</ul>

<a asp-controller="Item" asp-action="Index">Back to List</a> *@
